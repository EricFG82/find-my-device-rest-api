# Environment variables for Google Find My Device REST API

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Cache time-to-live in seconds
CACHE_TTL=60

# API host (usually 0.0.0.0 for Docker)
API_HOST=0.0.0.0

# API port
API_PORT=8000

